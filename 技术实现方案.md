# 啡阅（BrewRead）技术实现方案

> 基于 `需求.md` v2.0 与 `啡阅_UI设计建议.md` 修订  
> 平台：HarmonyOS NEXT  
> 当前工程框架：ArkTS + ArkUI + `@hadss/hmrouter` + 四 Tab 主框架雏形  
> 技术栈：ArkTS、ArkUI、RelationalStore、应用沙箱文件存储、本地 PDF 导出

---

## 1. 项目定位与实现原则

啡阅是一款本地单机阅读日记应用，核心闭环是：

```text
添加书籍 → 写阅读记录 → 按书/日期回顾 → 导出 PDF 成册
```

实现原则：

- **本地优先**：书籍、记录、图片、PDF、设置均保存在设备本地。
- **记录优先**：最高频页面是写记录页，交互与性能优先保障。
- **咖啡点缀**：咖啡作为阅读记录字段，不独立建复杂咖啡管理模块。
- **PDF 是出口**：导出 PDF 是产品差异化能力，需要尽早做基础原型。
- **克制开发**：不做登录注册、云同步、社交、AI 推荐、地图探店。

---

## 2. 当前工程现状

当前工程已经具备 HarmonyOS 应用基本框架：

```text
entry/src/main/ets/
├── entryability/EntryAbility.ets
├── entrybackupability/EntryBackupAbility.ets
├── pages/
│   ├── launchPage.ets
│   ├── homePage.ets
│   └── Index.ets
├── feature/
│   ├── bookShelfPage.ets
│   ├── journalPage.ets
│   ├── calenderPage.ets
│   └── mePage.ets
└── pagePath.ets
```

已完成或已具备：

- `@hadss/hmrouter` 依赖已接入。
- `launchPage` 作为根导航与启动入口。
- `homePage` 已开始改造为底部 4 Tab 主框架。
- 已新增书架、记录、日历、我的四个 Feature 页面占位。
- 已加入底部 Tab 图标资源，包括普通态与激活态。
- `EntryBackupAbility` 与备份配置已有预留。
- `EntryAbility` 已处理沉浸式窗口与安全区高度写入 `AppStorage`。

当前主要差距：

- Feature 页面仍是占位状态。
- 尚未建立数据模型、数据库、服务层、组件层。
- 路由常量仍较少，子页面路径未定义。
- 安全区键名需要统一封装。
- UI Design Tokens 尚未形成可复用常量。

---

## 3. 需求修订建议

### 3.1 页面数量修订

需求文档中写「所有页面（共10个）」，但实际表格包含 12 个页面：

- 书架：3 个
- 记录：4 个
- 日历：2 个
- 我的：3 个

建议修订为：

> 所有页面（共12个）

不建议为了凑 10 个页面而合并功能，因为书籍详情、日记本、设置、导出管理边界清晰，拆开更利于路由和维护。

### 3.2 MVP 范围修订

需求中「首版即全部，一次做完」包含图片、PDF、导出历史、备份恢复、日历热力、ISBN 扫描等能力，开发风险较高。建议保持最终目标不变，但按可验证闭环分阶段交付。

推荐分为：

- **MVP 1.0 核心闭环**：书籍 CRUD、记录 CRUD、书籍日记本、基础日历、基础统计、基础 PDF 导出。
- **MVP 1.1 体验增强**：图片拍照/选图、导出历史、PDF 样式优化、设置页。
- **MVP 1.2 数据能力**：备份导出/恢复、ISBN 扫描识别。

### 3.3 ISBN 离线能力边界

「扫描 ISBN 条形码自动填充信息」在纯离线场景不可完整实现。条形码只能识别 ISBN 号，无法离线获取书名、作者等元数据，除非内置大型书籍库。

建议改为：

> 可选：扫描 ISBN 条形码，仅自动填入 ISBN 号；书名、作者仍由用户手动填写。

数据模型中保留 `isbn` 字段，方便未来接入联网书籍信息查询，但当前版本不承诺自动补全。

### 3.4 UI 需求修订

`啡阅_UI设计建议.md` 中包含登录/注册页面，但 `需求.md` 明确本地单机、无需账号。为保持产品边界，技术方案不实现登录注册。

保留 UI 建议中的以下内容：

- 晨雾 / 夜幕双主题。
- 陶土红、鼠尾草绿、暖杏金配色体系。
- 写记录页作为 P0 页面。
- 悬浮胶囊 Tab 栏。
- 书架卡片、日记本时间轴、日历热力图。
- 极简留白杂志风 PDF。

不采纳：

- 登录页面。
- 注册页面。
- 验证码、用户协议、隐私政策登录流程。

---

## 4. 推荐工程目录

在当前工程基础上渐进扩展，不重建项目：

```text
entry/src/main/ets/
├── entryability/
├── entrybackupability/
├── pages/
│   ├── launchPage.ets
│   ├── homePage.ets
│   ├── bookshelf/
│   │   ├── addBookPage.ets
│   │   ├── bookDetailPage.ets
│   │   └── editBookPage.ets
│   ├── journal/
│   │   ├── entryEditPage.ets
│   │   └── bookJournalPage.ets
│   ├── calendar/
│   │   └── dayDetailPage.ets
│   └── me/
│       ├── exportManagerPage.ets
│       └── settingsPage.ets
├── feature/
│   ├── bookShelfPage.ets
│   ├── journalPage.ets
│   ├── calenderPage.ets
│   └── mePage.ets
├── components/
│   ├── common/
│   ├── bookshelf/
│   ├── journal/
│   ├── calendar/
│   └── me/
├── models/
├── services/
│   ├── database/
│   ├── repository/
│   ├── file/
│   ├── export/
│   └── statistics/
├── stores/
├── theme/
├── utils/
└── pagePath.ets
```

说明：

- `feature/` 保留四个一级 Tab 页面。
- `pages/` 放可路由子页面。
- `components/` 放可复用 UI 组件。
- `models/` 放领域模型与枚举。
- `services/database/` 负责 RelationalStore 初始化与迁移。
- `services/repository/` 负责表级 CRUD。
- `services/export/` 负责 PDF 导出。
- `theme/` 负责 Design Tokens 与浅/深色主题。
- `utils/` 放日期、校验、文件名清洗等工具。

---

## 5. 路由规划

当前 `PAGE_PATH` 已有：

```text
MAIN_PAGE
LAUNCH_PAGE
HOME_PAGE
FEATURE_BOOKSHELF_PAGE
FEATURE_CALENDER_PAGE
FEATURE_JOURNAL_PAGE
FEATURE_ME_PAGE
```

建议补齐：

```text
BOOK_ADD_PAGE
BOOK_DETAIL_PAGE
BOOK_EDIT_PAGE
ENTRY_NEW_PAGE
ENTRY_EDIT_PAGE
BOOK_JOURNAL_PAGE
DAY_DETAIL_PAGE
EXPORT_MANAGER_PAGE
SETTINGS_PAGE
PDF_PREVIEW_PAGE
```

路由规则：

- 路由常量统一放在 `pagePath.ets`。
- 页面统一使用 `@HMRouter` 注解。
- 页面跳转统一使用 `HMRouterMgr.push`。
- 不混用系统 `router` 与 HMRouter。
- 参数传递统一使用对象参数，如 `{ bookId: string }`、`{ entryId: string }`。

---

## 6. UI 技术落地

### 6.1 主题系统

建议建立 `theme/ThemeTokens.ets`，按浅色/深色输出颜色、间距、圆角、字号。

亮色晨雾主题：

| Token | 值 |
|---|---|
| 页面底色 | `#F6F4F0` |
| 卡片底 | `#FFFFFF` |
| 输入区底 | `#F1EEE8` |
| 主色 | `#C2865E` |
| 深主色 | `#9B5C3E` |
| 点缀绿 | `#8EA590` |
| 正文 | `#2C2825` |
| 辅助文字 | `#7D7872` |

暗色夜幕主题：

| Token | 值 |
|---|---|
| 页面底色 | `#1A1C1D` |
| 卡片底 | `#242628` |
| 输入区底 | `#2A2C2E` |
| 主色 | `#D4A882` |
| 次主色 | `#E2BFA0` |
| 点缀绿 | `#A3B5A6` |
| 正文 | `#E8E4DF` |
| 辅助文字 | `#9E9992` |

### 6.2 全局组件优先级

优先实现以下组件：

1. `SafeAreaContainer`：统一顶部/底部安全区。
2. `BrewCard`：通用卡片容器。
3. `EmptyState`：空状态。
4. `BookCard`：书籍卡片。
5. `ReadingEntryCard`：阅读记录卡片。
6. `MoodSelector`：心情选择器。
7. `PageHeader`：页面标题栏。
8. `FloatingActionButton`：写记录/添加书籍入口。
9. `CalendarHeatMap`：日历热力组件。
10. `StatCard`：我的页统计卡片。

### 6.3 写记录页体验要求

写记录页是 P0 页面，需优先满足：

- 默认选中最近使用书籍。
- 起始页默认填上次 `pageTo`。
- 结束页输入后实时计算「今日 X 页」。
- 必填仅为「书籍 + 心情 + 感悟」。
- 咖啡、页码、照片均为可选。
- 感悟输入区最小高度不小于 200vp。
- 保存失败时保留表单内容。
- 保存成功后刷新时间线并返回上一页。

### 6.4 底部 Tab

当前 `homePage.ets` 已基本符合悬浮胶囊 Tab 方向，后续优化：

- 颜色接入主题 Token，不写死单一浅色值。
- Tab 容器支持暗色毛玻璃效果。
- 选中态使用主题主色低透明背景。
- 写记录、PDF 预览等全屏页面隐藏底部 Tab。

---

## 7. 数据模型

### 7.1 Book

```text
Book
├── id: string
├── name: string
├── author?: string
├── isbn?: string
├── totalPages?: number
├── coverPath?: string
├── status: reading | finished | want
├── sortOrder: number
├── createdAt: number
└── updatedAt: number
```

### 7.2 ReadingEntry

```text
ReadingEntry
├── id: string
├── bookId: string
├── date: number
├── pageFrom?: number
├── pageTo?: number
├── mood: happy | calm | neutral | sad | pensive
├── coffee?: string
├── content: string
├── photoPath?: string
├── createdAt: number
└── updatedAt: number
```

### 7.3 ExportRecord

```text
ExportRecord
├── id: string
├── bookId: string
├── filePath: string
├── paperSize: A4 | A5
├── fileName: string
├── createdAt: number
└── updatedAt: number
```

### 7.4 AppSetting

```text
AppSetting
├── key: string
├── value: string
└── updatedAt: number
```

---

## 8. 数据库设计

### 8.1 books 表

```sql
CREATE TABLE IF NOT EXISTS books (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  author TEXT,
  isbn TEXT,
  total_pages INTEGER,
  cover_path TEXT,
  status TEXT NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_books_status ON books(status);
CREATE INDEX IF NOT EXISTS idx_books_sort_order ON books(sort_order ASC);
CREATE INDEX IF NOT EXISTS idx_books_created_at ON books(created_at DESC);
```

### 8.2 reading_entries 表

```sql
CREATE TABLE IF NOT EXISTS reading_entries (
  id TEXT PRIMARY KEY,
  book_id TEXT NOT NULL,
  date INTEGER NOT NULL,
  page_from INTEGER,
  page_to INTEGER,
  mood TEXT NOT NULL,
  coffee TEXT,
  content TEXT NOT NULL,
  photo_path TEXT,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  FOREIGN KEY(book_id) REFERENCES books(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_entries_book_id ON reading_entries(book_id);
CREATE INDEX IF NOT EXISTS idx_entries_date ON reading_entries(date DESC);
CREATE INDEX IF NOT EXISTS idx_entries_book_date ON reading_entries(book_id, date DESC);
```

### 8.3 export_records 表

```sql
CREATE TABLE IF NOT EXISTS export_records (
  id TEXT PRIMARY KEY,
  book_id TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_name TEXT NOT NULL,
  paper_size TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  FOREIGN KEY(book_id) REFERENCES books(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_export_records_book_id ON export_records(book_id);
CREATE INDEX IF NOT EXISTS idx_export_records_created_at ON export_records(created_at DESC);
```

### 8.4 app_settings 表

```sql
CREATE TABLE IF NOT EXISTS app_settings (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at INTEGER NOT NULL
);
```

---

## 9. 数据校验规则

### 9.1 书籍

| 字段 | 规则 |
|---|---|
| 书名 | 必填，1-100 字 |
| 作者 | 可选，最多 80 字 |
| ISBN | 可选，10 或 13 位数字，存储前移除连字符和空格 |
| 总页数 | 可选，1-99999 |
| 状态 | `reading` / `finished` / `want`，默认 `reading` |

### 9.2 阅读记录

| 字段 | 规则 |
|---|---|
| 关联书籍 | 必填，必须存在 |
| 心情 | 必填，默认 `calm` |
| 感悟 | 必填，1-5000 字 |
| 咖啡 | 可选，最多 50 字 |
| 起始页 | 可选，填写时必须大于 0 |
| 结束页 | 可选，必须大于等于起始页 |
| 日期 | 默认当前时间，允许补记过去日期 |
| 照片 | 可选，保存失败不影响文本记录保存 |

---

## 10. 核心模块实现方案

### 10.1 书架模块

功能：

- 三态 Tab：在读、已读完、想读。
- 添加、编辑、删除书籍。
- 书籍卡片展示封面、书名、作者、进度、记录数。
- 无封面时展示书名首字渐变占位。
- 删除书籍时二次确认，并级联删除记录与图片。

进度计算：

```text
currentPage = max(page_to) by book_id
progress = currentPage / totalPages
```

若 `currentPage >= totalPages`，提示用户是否将状态改为已读完。

### 10.2 阅读记录模块

功能：

- 全部记录时间线，按日期倒序。
- 写记录页。
- 编辑记录页。
- 删除记录。
- 按书籍过滤记录。

保存流程：

```text
校验表单 → 保存图片到沙箱（可选） → 写入 reading_entries → 刷新统计 → 返回来源页
```

### 10.3 日记本模块

功能：

- 展示单本书完整阅读旅程。
- 顶部展示书名、作者、记录数、咖啡杯数、阅读跨度。
- 记录以时间轴卡片展示。
- 支持展开全文、编辑、删除。
- 底部固定「导出 PDF」按钮。

### 10.4 日历模块

按日期聚合字段：

```text
dateKey = yyyy-MM-dd
entryCount = 当天记录数
wordCount = 当天 content 总字数
coffeeCount = coffee 非空记录数
mainMood = 当天出现最多的 mood
```

热力强度：

```text
intensity = min(4, ceil(wordCount / 300))
```

交互：

- 左右切换月份。
- 点击有记录日期显示摘要。
- 点击空白日期显示补记入口，并进入写记录页预填日期。

### 10.5 我的模块

功能：

- 阅读天数。
- 总记录数。
- 在读/已读/想读统计。
- 咖啡记录数。
- 心情分布。
- 导出历史入口。
- 设置入口。

统计均从数据库动态聚合，不额外冗余存储。

---

## 11. 文件存储方案

应用沙箱文件结构：

```text
files/
├── covers/
│   └── {bookId}.jpg
├── entries/
│   └── {entryId}.jpg
├── exports/
│   └── {exportId}.pdf
└── backups/
    └── BrewRead_Backup_{yyyyMMdd_HHmm}.zip
```

规则：

- 拍照或选图后复制到应用沙箱。
- 封面图片建议压缩至 600px 宽。
- 记录照片建议压缩至 1280px 宽。
- 删除书籍时删除封面与关联记录照片。
- 删除记录时删除对应记录照片。
- 导出历史文件缺失时标记为「文件已不存在」。

---

## 12. PDF 导出方案

### 12.1 服务拆分

```text
PdfExportService
├── loadBookAndEntries()
├── buildDocumentModel()
├── layoutPages()
├── writePdfFile()
└── saveExportRecord()

PdfLayoutEngine
├── calculatePageSize()
├── measureText()
├── wrapText()
├── paginateBlocks()
└── drawPage()
```

### 12.2 样式

采用 UI 建议中的「极简留白杂志风」：

| 元素 | 样式 |
|---|---|
| 封面底色 | `#FFFFFF` |
| 标题色 | `#9B5C3E` |
| 正文色 | `#2C2825` |
| 装饰色 | `#C2865E` |
| 正文行高 | 1.8 |
| 页边距 | A4 48pt / A5 36pt |

### 12.3 内容顺序

1. 封面页：书名、作者、年份、共读次数、咖啡次数、生成日期。
2. 正文页：日期、心情、咖啡、页码范围、感悟全文、照片。
3. 页脚：页码，格式 `- 3 -`。

### 12.4 文件命名

推荐：

```text
《书名》阅读日记_yyyyMMdd_HHmm.pdf
```

文件名保存前需要清洗系统非法字符。

### 12.5 异常处理

- 无记录时禁止导出并提示。
- 导出过程中禁用重复点击。
- 图片加载失败时跳过图片并继续导出。
- 磁盘空间不足时提示失败原因。
- 导出成功后提供预览、分享、保存入口。

---

## 13. 备份恢复方案

备份文件格式：`.zip`

内容：

```text
books.json
entries.json
export_records.json
settings.json
metadata.json
images/
├── covers/
└── entries/
```

`metadata.json` 包含：

```text
appVersion
dataVersion
exportedAt
platform
```

实现顺序：

1. MVP 先完成数据库与文件结构。
2. MVP 1.1 可做备份导出。
3. MVP 1.2 再做恢复与合并策略。

恢复策略：

- 先校验版本。
- 再导入临时数据。
- 用户选择覆盖或合并。
- 最后统一写入数据库与图片目录。

---

## 14. 权限策略

| 功能 | 权限/能力 | 申请时机 |
|---|---|---|
| 拍封面 | 相机 | 点击拍照时 |
| 拍阅读场景 | 相机 | 点击拍照时 |
| 从相册选图 | 媒体访问 | 点击选图时 |
| 保存 PDF | 文件保存 Picker | 点击保存到文件时 |
| 分享 PDF | 系统分享 | 点击分享时 |
| 打印 PDF | 系统分享/打印能力 | 点击打印时 |

原则：

- 不在首次启动一次性申请权限。
- 权限拒绝后提供跳过或手动输入路径。
- 图片失败不阻塞文本记录。

---

## 15. 错误处理与空状态

### 15.1 空状态文案

| 场景 | 文案 |
|---|---|
| 书架为空 | 还没有书，添加第一本开始记录吧 |
| 想读为空 | 标记你想读的书，它会在书架上等你 |
| 记录为空 | 今天还没有写下阅读感悟 |
| 某书无记录 | 这本书还没有阅读日记 |
| 日历当月无记录 | 这个月还没有阅读足迹 |
| 导出历史为空 | 导出的阅读手账会出现在这里 |

### 15.2 常见错误

| 错误 | 处理 |
|---|---|
| 数据库初始化失败 | Toast 提示重启，并记录日志 |
| 保存失败 | Toast 提示，保留表单内容 |
| 图片保存失败 | 提示图片未保存，但允许保存文本 |
| PDF 导出失败 | 展示失败原因，允许重试 |
| 文件不存在 | 导出历史中标记「文件已不存在」 |

---

## 16. 按顺序执行的开发计划表

以下计划按「完成需求文档」所需能力拆解，建议严格从上到下执行。每一步完成后都应能运行或验证，避免最后集中集成。

| 顺序 | 阶段 | 任务 | 产出 | 验收标准 |
|---:|---|---|---|---|
| 1 | 工程整理 | 统一 `pagePath.ets` 路由命名，补齐所有页面路径 | 完整路由常量 | 所有页面路径集中管理，无硬编码路径 |
| 2 | 工程整理 | 封装安全区容器，统一 `topH` / `bottomH` 使用 | `SafeAreaContainer` | 页面不重复处理安全区，顶部底部无遮挡 |
| 3 | 工程整理 | 建立 `components`、`models`、`services`、`theme`、`utils` 目录 | 标准目录结构 | 后续功能有明确落位 |
| 4 | 主题系统 | 实现晨雾/夜幕 Design Tokens | 主题常量 | 页面可读取统一颜色、间距、圆角 |
| 5 | 主框架 | 优化 `homePage` 悬浮胶囊 Tab | 四 Tab 主框架 | 书架/记录/日历/我的可正常切换 |
| 6 | 数据模型 | 定义 `Book`、`ReadingEntry`、`ExportRecord`、`AppSetting` | 模型与枚举 | 字段覆盖需求文档 |
| 7 | 数据库 | 初始化 RelationalStore 与数据库版本 | 数据库服务 | App 启动可初始化数据库 |
| 8 | 数据库 | 创建 4 张表与索引 | 表结构迁移 | 重启 App 后表结构稳定存在 |
| 9 | Repository | 实现书籍 CRUD | `BookRepository` | 可新增、查询、编辑、删除书籍 |
| 10 | Repository | 实现阅读记录 CRUD | `ReadingEntryRepository` | 可新增、查询、编辑、删除记录 |
| 11 | Repository | 实现导出历史与设置读写 | `ExportRecordRepository`、`SettingRepository` | 可保存导出记录和默认设置 |
| 12 | 书架 | 实现书架首页三态列表 | `bookShelfPage` | 在读/已读/想读可切换并展示数据 |
| 13 | 书架 | 实现书籍卡片与无封面占位 | `BookCard` | 卡片展示封面、书名、作者、进度、记录数 |
| 14 | 书架 | 实现添加书籍页 | `addBookPage` | 必填校验通过后可新增书籍 |
| 15 | 书架 | 实现编辑书籍页 | `editBookPage` | 可修改书籍信息与状态 |
| 16 | 书架 | 实现删除书籍与级联删除 | 删除流程 | 二次确认后删除书籍、记录与图片 |
| 17 | 记录 | 实现心情选择器 | `MoodSelector` | 5 种心情可选，选中态清晰 |
| 18 | 记录 | 实现写记录页基础表单 | `entryEditPage` 新增模式 | 可选择书籍、心情、填写感悟并保存 |
| 19 | 记录 | 实现页码与咖啡字段 | 表单增强 | 页码规则校验，咖啡可选保存 |
| 20 | 记录 | 实现智能预填 | 表单体验 | 默认选最近书籍，起始页预填上次结束页 |
| 21 | 记录 | 实现全部记录时间线 | `journalPage` | 所有记录按日期倒序展示 |
| 22 | 记录 | 实现编辑记录页 | `entryEditPage` 编辑模式 | 已有记录可回填、修改、保存 |
| 23 | 记录 | 实现删除记录 | 删除流程 | 二次确认后记录删除，列表刷新 |
| 24 | 日记本 | 实现书籍详情页 | `bookDetailPage` | 展示书籍信息与该书记录列表 |
| 25 | 日记本 | 实现某书日记本页 | `bookJournalPage` | 按书聚合展示完整阅读旅程 |
| 26 | 日记本 | 实现记录展开/收起 | `ReadingEntryCard` | 长内容可展开全文并收起 |
| 27 | 统计 | 实现基础统计服务 | `StatisticsService` | 能计算书籍数、记录数、阅读天数、心情分布 |
| 28 | 日历 | 实现月视图日期计算 | 日历工具 | 任意月份布局正确，支持闰年 |
| 29 | 日历 | 实现日历热力图 | `CalendarHeatMap` | 有记录日期显示热力点与心情色 |
| 30 | 日历 | 实现日期摘要 | `calenderPage` | 点击日期展示当天记录摘要 |
| 31 | 日历 | 实现日期详情页 | `dayDetailPage` | 展示当天完整记录，可进入编辑 |
| 32 | 日历 | 实现空白日期补记 | 补记入口 | 点击空白日期可带日期进入写记录页 |
| 33 | 我的 | 实现我的首页统计 | `mePage` | 展示阅读天数、记录数、书籍状态、心情分布 |
| 34 | 设置 | 实现设置页 | `settingsPage` | 可设置主题、默认纸张、默认字体 |
| 35 | 图片 | 实现图片目录管理 | 文件服务 | 可创建 covers/entries/exports 目录 |
| 36 | 图片 | 实现封面拍照/选图 | 书籍图片能力 | 可保存封面到沙箱并回显 |
| 37 | 图片 | 实现记录照片拍照/选图 | 记录图片能力 | 可保存场景照片到沙箱并回显 |
| 38 | 图片 | 实现图片删除清理 | 文件清理 | 删除书籍/记录时同步清理图片 |
| 39 | PDF | 实现 PDF 导出数据查询 | `PdfExportService` | 可获取书籍、记录、统计摘要 |
| 40 | PDF | 实现 PDF 文档模型 | 文档模型 | 封面、正文、图片块结构清晰 |
| 41 | PDF | 实现文本测量与换行 | `PdfLayoutEngine` | 长文本不会溢出页面 |
| 42 | PDF | 实现封面页绘制 | PDF 封面 | 包含书名、作者、年份、统计 |
| 43 | PDF | 实现正文页绘制 | PDF 正文 | 每条记录包含日期、心情、咖啡、页码、感悟 |
| 44 | PDF | 实现自动分页 | 分页能力 | 超长内容可跨页展示 |
| 45 | PDF | 实现图片插入 | 图片能力 | 有照片时可显示，失败时跳过 |
| 46 | PDF | 实现文件保存与命名 | PDF 文件 | 生成 `《书名》阅读日记_yyyyMMdd_HHmm.pdf` |
| 47 | PDF | 实现导出历史 | `exportManagerPage` | 导出成功后历史列表可查看 |
| 48 | PDF | 实现分享/保存入口 | 系统能力 | 导出后可分享或保存到用户位置 |
| 49 | 备份 | 实现备份导出 | 备份 zip | 可导出 JSON 与图片文件 |
| 50 | 备份 | 实现备份恢复 | 恢复流程 | 可校验版本并恢复数据 |
| 51 | ISBN | 实现 ISBN 扫描识别 | 可选能力 | 仅自动填 ISBN，不承诺自动补全书籍信息 |
| 52 | 打磨 | 完善空状态、加载态、错误态 | 全局体验 | 各页面无空白尴尬态 |
| 53 | 打磨 | 完成暗色主题适配 | 双主题 | 浅色/深色/跟随系统可用 |
| 54 | 测试 | 数据 CRUD 测试 | 测试结果 | 新增、编辑、删除、重启后数据正确 |
| 55 | 测试 | 日历边界测试 | 测试结果 | 跨月、闰年、周起始布局正确 |
| 56 | 测试 | PDF 边界测试 | 测试结果 | 无记录、长文本、多图、A4/A5 均可处理 |
| 57 | 测试 | 真机权限与文件测试 | 测试结果 | 权限拒绝、文件丢失、空间不足有提示 |
| 58 | 发布前 | 全量回归与性能优化 | 可发布包 | 核心流程稳定，无明显卡顿 |

---

## 17. 测试清单

### 17.1 数据测试

- 新增、编辑、删除书籍。
- 新增、编辑、删除阅读记录。
- 删除书籍后关联记录是否删除。
- 页码非法输入是否拦截。
- App 重启后数据是否仍存在。

### 17.2 UI 测试

- 四 Tab 切换是否流畅。
- 浅色/深色主题下文字对比度是否足够。
- 底部胶囊 Tab 是否避让系统手势条。
- 写记录页键盘弹出时是否遮挡输入区。
- 空状态与错误态是否完整。

### 17.3 日历测试

- 单日单条记录。
- 单日多条记录。
- 跨月记录。
- 闰年 2 月。
- 月份首日为周一/周日等不同布局。

### 17.4 PDF 测试

- 无记录导出。
- 短记录导出。
- 超长文本导出。
- 带图片导出。
- A4/A5 两种纸张导出。
- 文件名包含特殊字符的书名导出。

### 17.5 文件测试

- 图片文件丢失。
- PDF 文件被外部删除。
- 沙箱空间不足。
- 备份文件版本不匹配。

---

## 18. 技术风险与规避

| 风险 | 影响 | 规避方案 |
|---|---|---|
| PDF 自绘排版复杂 | 高 | 先做纯文本基础版，再加图片和样式 |
| 离线 ISBN 自动补全不可行 | 中 | 仅识别 ISBN 号，不承诺补全书籍信息 |
| 图片体积过大 | 中 | 统一压缩并限制最大尺寸 |
| 数据误删 | 中 | 删除二次确认，后续提供备份恢复 |
| 日历热力规则不明确 | 低 | 以字数为主、记录数为辅 |
| 备份恢复复杂 | 中 | 先导出备份，再实现恢复 |
| 双主题工作量增加 | 中 | 先建立 Token，页面开发时同步使用 |

---

## 19. 最终技术摘要

| 层级 | 方案 | 说明 |
|---|---|---|
| 语言 | ArkTS | HarmonyOS NEXT 原生开发 |
| UI | ArkUI 声明式 | 页面与组件拆分 |
| 路由 | `@hadss/hmrouter` | 统一注解与 `PAGE_PATH` 管理 |
| 状态 | 页面状态 + 轻量 Store | 不引入重型状态框架 |
| 数据库 | RelationalStore | `books` / `reading_entries` / `export_records` / `app_settings` |
| 文件 | 应用沙箱 | 封面、记录照片、PDF、备份分目录 |
| PDF | 本地导出服务 | 封面、正文、自动分页、图片插入 |
| 主题 | Design Tokens | 晨雾浅色 / 夜幕深色 |
| 备份 | JSON + images zip | 后续增强 |
| ISBN | 本地识别 ISBN 号 | 不离线补全书籍元数据 |

---

## 20. 结论

啡阅当前项目已经完成主框架雏形，适合继续沿现有 `launchPage` + `homePage` + 四 Feature Tab + HMRouter 的方向推进。技术实现上应先补齐目录、主题、模型、数据库与 Repository，再按「书籍 → 记录 → 日记本 → PDF」打通核心闭环，随后补齐日历、我的统计、图片、导出历史、备份与 ISBN。

---

## 21. 实现记录

### 21.1 2026-08-03：一级 Tab 页面 UI 绘制

实现范围：

- 完成 `feature/bookShelfPage.ets` 书架一级页面静态 UI。
- 完成 `feature/journalPage.ets` 阅读记录时间线静态 UI。
- 完成 `feature/calenderPage.ets` 日历热力图静态 UI。
- 完成 `feature/mePage.ets` 我的页统计与设置入口静态 UI。

排查备注：

- 本阶段只绘制 UI，不接入数据库。
- 底部 Tab 切换逻辑保持原状，未改动 `homePage.ets` 的 Tab 行为。

### 21.2 2026-08-03：路由、主题、模型、工具层

实现范围：

- 补齐 `pagePath.ets` 中二级页面路由常量。
- 新增 `theme/ThemeTokens.ets`，包含浅色与深色主题 Token。
- 新增 `models/BookModel.ets`、`models/ReadingEntryModel.ets`、`models/ExportRecordModel.ets`、`models/AppSettingModel.ets`。
- 新增 `utils/ValidationUtils.ets`、`utils/DateUtils.ets`、`utils/BookUtils.ets`。

排查备注：

- 模型字段采用 ArkTS 驼峰命名，数据库字段采用下划线命名。
- 页面后续应逐步使用主题 Token，减少硬编码颜色。

### 21.3 2026-08-03：数据库 Schema 与初始化管理器

实现范围：

- 新增 `services/database/DatabaseSchema.ets`。
- 新增 `services/database/DatabaseManager.ets`。
- 建立 `books`、`reading_entries`、`export_records`、`app_settings` 四张表的建表 SQL。
- 建立书籍、记录、导出历史相关索引 SQL。

排查备注：

- `DatabaseManager.init(context)` 负责打开 RelationalStore 并执行建表 SQL。
- `DatabaseManager.getStore()` 要求数据库已初始化，Repository 使用前需确保启动阶段已调用 init。

### 21.4 2026-08-03：应用启动初始化数据库与 Repository 基础层

实现范围：

- 修改 `entryability/EntryAbility.ets`，在应用 `onCreate` 阶段调用 `DatabaseManager.init(this.context)`。
- 新增 `services/repository/BookRepository.ets`，提供书籍新增、更新、删除、按状态查询、按 id 查询能力。
- 新增 `services/repository/ReadingEntryRepository.ets`，提供阅读记录新增、更新、删除、全部查询、按书查询、按日期范围查询能力。
- 新增阅读记录统计方法：按书获取最大 `pageTo`、按书统计记录数。

排查备注：

- Repository 层只负责数据读写和模型映射，页面层不直接操作 RdbStore。
- 当前统计方法先基于查询结果计算，后续数据量增大后可替换为 SQL 聚合查询。
- 数据库初始化失败时只记录日志，不阻塞窗口加载；若后续页面数据操作报错，优先检查启动日志中的 Database init 信息。

### 21.5 2026-08-03：添加书籍页面入口与保存流程

实现范围：

- 新增 `utils/IdUtils.ets`，提供本地实体 id 生成工具。
- 新增 `pages/bookshelf/addBookPage.ets`，实现添加书籍表单 UI。
- 添加书籍页接入 `ValidationUtils`，支持书名、作者、ISBN、总页数、书籍状态校验。
- 添加书籍页接入 `BookRepository.insertBook()`，校验通过后写入本地数据库。
- 修改 `feature/bookShelfPage.ets`，顶部加号和底部“添加新书”虚线卡片跳转到添加书籍页。

排查备注：

- 添加页保存成功后使用 `HMRouterMgr.pop()` 返回书架页。
- 当前书架页仍使用静态示例数据，下一步需要在 `aboutToAppear` 中调用 `BookRepository.queryBooksByStatus()` 加载真实数据。
- 如果点击添加入口没有跳转，优先检查 `PAGE_PATH.BOOK_ADD_PAGE` 是否与 `addBookPage.ets` 的 `@HMRouter` 页面路径一致。
- 如果保存失败，优先检查数据库是否已在 `EntryAbility.onCreate` 成功初始化。

### 21.6 2026-08-03：书架页接入真实数据库数据

实现范围：

- 修改 `feature/bookShelfPage.ets`，移除静态示例书籍数据。
- 书架页在 `aboutToAppear` 中调用 `loadBooks()`，按当前状态 Tab 加载真实书籍。
- 三态 Tab 切换时调用 `BookRepository.queryBooksByStatus()` 查询 `reading`、`finished`、`want` 对应列表。
- 书架卡片接入 `ReadingEntryRepository.getMaxPageToByBookId()` 计算当前阅读页。
- 书架卡片接入 `ReadingEntryRepository.countEntriesByBookId()` 展示真实记录数。
- 书架卡片使用 `BookUtils.calculateProgress()` 和 `BookUtils.getProgressText()` 生成进度条和进度文案。
- 新增加载中、加载失败、空状态展示。

排查备注：

- 如果添加书籍后返回书架未展示，优先检查 `aboutToAppear` 是否触发，以及 `BookRepository.queryBooksByStatus()` 查询的状态是否与添加页保存的状态一致。
- 如果进度始终为 0，属于正常阶段性现象；当前还未实现写记录页，`reading_entries` 表没有真实记录。
- 如果记录数不正确，优先检查 `ReadingEntryRepository.queryEntriesByBookId()` 的 `book_id` 是否与书籍 id 一致。
- 当前每本书统计会查询两次记录表，数据量增大后建议增加 Repository 聚合查询优化性能。

### 21.7 2026-08-03：写阅读记录页面与记录入口

实现范围：

- 新增 `pages/journal/entryEditPage.ets`，实现写阅读记录表单页面。
- 写记录页从 `BookRepository` 加载“在读”和“已读完”书籍作为候选，不展示“想读”书籍。
- 写记录页支持选择书籍、选择心情、填写起始页、结束页、咖啡和阅读感悟。
- 写记录页接入 `ValidationUtils`，校验书籍、心情、咖啡、页码范围和感悟内容。
- 写记录页接入 `ReadingEntryRepository.insertEntry()`，保存成功后返回上一页。
- 修改 `feature/journalPage.ets`，让顶部“写记录”按钮跳转 `PAGE_PATH.ENTRY_EDIT_PAGE`。

排查备注：

- 如果写记录页没有可选书籍，优先确认书架中是否存在 `reading` 或 `finished` 状态书籍。
- 如果保存记录后书架进度没有变化，优先检查记录的 `bookId` 是否为当前书籍 id，以及 `pageTo` 是否大于 0。
- 当前记录页时间线仍是静态示例数据，下一步需要接入 `ReadingEntryRepository.queryAllEntries()` 展示真实记录。
- `ENTRY_EDIT_PAGE` 当前同时作为新建记录页面使用，后续编辑记录时可通过路由参数传入 `entryId` 区分模式。

### 21.8 2026-08-03：记录页接入真实时间线数据

实现范围：

- 修改 `feature/journalPage.ets`，移除静态示例记录数据。
- 记录页在 `aboutToAppear` 中调用 `loadEntries()`，从 `ReadingEntryRepository.queryAllEntries()` 读取真实阅读记录。
- 记录页根据每条记录的 `bookId` 调用 `BookRepository.queryBookById()` 补充书籍名称。
- 记录页使用 `DateUtils.formatMonthDay()` 和 `DateUtils.getWeekdayText()` 展示日期。
- 记录页使用 `getMoodText()` 和 `getMoodColor()` 展示心情文案与心情色条。
- 顶部摘要栏改为基于真实记录计算：记录总数、配咖啡次数、阅读页数。
- 新增加载中、加载失败、空状态展示，空状态可直接跳转写记录页。

排查备注：

- 如果记录页为空，优先确认 `reading_entries` 表是否已有保存成功的记录。
- 如果记录卡片显示“已删除书籍”，说明记录的 `bookId` 在 `books` 表中找不到对应书籍。
- 如果阅读页数统计为 0，优先检查记录是否填写了起始页和结束页；当前只在两者都存在时计算页数。
- 当前每条记录都会补查一次书籍，记录数量增大后建议增加批量查询或缓存 bookId 到书籍名称的映射。

### 21.9 2026-08-03：日历页接入真实热力图数据

实现范围：

- 修改 `feature/calenderPage.ets`，移除静态日历热力点。
- 日历页在 `aboutToAppear` 中调用 `loadMonthData()`，根据当前年月查询当月阅读记录。
- 使用 `ReadingEntryRepository.queryEntriesByDateRange()` 查询当前月份第一天到下个月第一天前 1ms 的记录。
- 动态生成月份日历网格，按周一为首列补齐月份首日前的空白格。
- 根据每天记录数和阅读页数计算热力等级，渲染不同大小和透明度的热力圆点。
- 支持点击上一月、下一月切换月份并重新加载数据。
- 支持点击日期，底部摘要卡片展示当天第一条真实阅读记录。
- 底部月度统计展示本月阅读天数和配咖啡次数。
- 新增加载中和加载失败状态。

排查备注：

- 如果某天没有热力点，优先确认记录的 `date` 是否落在当前月份范围内。
- 如果月份首日错位，优先检查 `getMondayFirstWeekDay()`，当前日历以周一为第一列。
- 如果选中日期摘要为空但热力点存在，优先检查 `isSameDay()` 的年月日比较逻辑。
- 当前摘要只展示当天第一条记录，后续可升级为当天多条记录列表或跳转 `DAY_DETAIL_PAGE`。

### 21.10 2026-08-03：我的页接入真实统计数据

实现范围：

- 修改 `feature/mePage.ets`，移除静态概览、心情分布和月度趋势数据。
- 我的页在 `aboutToAppear` 中调用 `loadStatistics()` 读取真实统计。
- 使用 `BookRepository.queryBooksByStatus()` 统计在读书籍数和已读完书籍数。
- 使用 `ReadingEntryRepository.queryAllEntries()` 统计阅读记录总数。
- 基于全部阅读记录的 `mood` 字段计算心情分布百分比。
- 基于全部阅读记录的 `date` 字段计算近 6 个月每月阅读天数趋势。
- 新增加载中和加载失败状态，失败卡片可点击重试。

排查备注：

- 如果概览统计为 0，优先确认 `books` 表是否已有书籍，以及书籍状态是否为 `reading` 或 `finished`。
- 如果心情分布为空，优先确认 `reading_entries` 表是否已有记录。
- 如果月度趋势高度不变化，优先确认记录的 `date` 是否落在近 6 个月范围内。
- 当前趋势以“每月有记录的天数”为统计口径，而不是记录条数；同一天多条记录只计 1 天。

### 21.11 2026-08-03：书籍日记本页面与书架卡片跳转

实现范围：

- 新增 `pages/journal/bookJournalPage.ets`，展示某本书的阅读旅程时间线。
- 书籍日记本页通过 `AppStorage.selectedBookId` 获取书架入口传入的书籍 id。
- 书籍日记本页接入 `BookRepository.queryBookById()` 查询书籍名称和作者。
- 书籍日记本页接入 `ReadingEntryRepository.queryEntriesByBookId()` 查询该书全部阅读记录。
- 页面顶部摘要展示该书记录数、配咖啡次数和累计阅读页数。
- 时间线卡片展示日期、心情、咖啡、页码和阅读感悟。
- 修改 `feature/bookShelfPage.ets`，书架卡片点击后保存 `selectedBookId` 并跳转 `BOOK_JOURNAL_PAGE`。

排查备注：

- 如果点击书架卡片后提示“没有找到要查看的书籍”，优先检查 `openBookJournalPage()` 是否成功写入 `AppStorage.selectedBookId`。
- 如果提示“书籍不存在或已被删除”，说明 `selectedBookId` 在 `books` 表中没有匹配记录。
- 如果日记本页没有记录但记录页有记录，优先检查 `reading_entries.book_id` 是否等于当前书籍 id。
- 当前参数传递使用 AppStorage 兜底；后续确认 HMRouter 参数读取方式后，可替换为标准路由参数。

### 21.12 2026-08-03：书籍编辑与删除能力

实现范围：

- 新增 `pages/bookshelf/editBookPage.ets`，复用添加书籍的字段规则实现编辑表单。
- 编辑页通过 `AppStorage.selectedBookId` 获取当前书籍 id，并调用 `BookRepository.queryBookById()` 加载原始数据。
- 编辑页保存时调用 `BookRepository.updateBook()`，保持原 id、createdAt 和 sortOrder 不变。
- 修改 `pages/journal/bookJournalPage.ets`，顶部增加“编辑”和“删除”入口。
- 删除入口调用 `BookRepository.deleteBook()` 删除当前书籍，删除成功后返回上一页。

排查备注：

- 如果编辑页提示“没有找到要编辑的书籍”，优先检查进入编辑页前是否写入 `AppStorage.selectedBookId`。
- 如果编辑保存后关联记录丢失，优先检查是否错误生成了新的 book id；当前实现保持原 id 不变。
- 当前删除入口先直接删除，后续应补确认弹窗，避免误触。
- 数据库 Schema 已设置阅读记录外键级联删除，但如果设备环境未开启外键级联，后续需要在 Repository 中手动删除关联记录。

### 21.13 2026-08-03：阅读记录编辑与删除能力

实现范围：

- 修改 `services/repository/ReadingEntryRepository.ets`，新增 `queryEntryById(entryId)`。
- 改造 `pages/journal/entryEditPage.ets`，通过 `AppStorage.selectedEntryId` 判断新建或编辑模式。
- 编辑模式下加载原始阅读记录，回填书籍、心情、页码、咖啡和阅读感悟。
- 编辑保存时调用 `ReadingEntryRepository.updateEntry()`，保持原 id、date、createdAt 和 photoPath 不变。
- 新建保存时继续调用 `ReadingEntryRepository.insertEntry()`。
- 修改 `pages/journal/bookJournalPage.ets`，记录卡片增加“编辑”和“删除”入口。
- 删除单条记录时调用 `ReadingEntryRepository.deleteEntry()`，成功后重新加载当前书籍日记本统计。
- 修改新建记录入口，进入前清空 `AppStorage.selectedEntryId`，避免误入编辑模式。

排查备注：

- 如果写记录页误显示“编辑阅读记录”，优先检查入口是否清空 `AppStorage.selectedEntryId`。
- 如果编辑后时间线顺序变化，检查是否误改了原记录的 `date`；当前实现保留原 date。
- 如果删除记录后书架进度未变化，优先返回书架触发 `aboutToAppear`，或检查 `ReadingEntryRepository.getMaxPageToByBookId()` 查询结果。
- 当前记录删除仍是直接删除，后续需要补确认弹窗。

### 21.14 2026-08-03：删除确认与选中状态清理

实现范围：

- 修改 `pages/journal/bookJournalPage.ets`，为删除书籍增加底部确认面板。
- 修改 `pages/journal/bookJournalPage.ets`，为删除单条阅读记录增加底部确认面板。
- 删除书籍确认后调用 `BookRepository.deleteBook()`，成功后清空 `AppStorage.selectedBookId` 和 `AppStorage.selectedEntryId`。
- 删除阅读记录确认后调用 `ReadingEntryRepository.deleteEntry()`，成功后清空 `pendingDeleteEntryId` 和 `AppStorage.selectedEntryId`。
- 删除记录成功后重新调用 `loadJournal()` 刷新当前书籍记录数、咖啡数、阅读页数和时间线。

排查备注：

- 如果点击删除没有立即删除，属于预期行为；需要在确认面板中再次点击危险按钮。
- 如果确认面板无法关闭，优先检查 `showDeleteBookConfirm` 或 `showDeleteEntryConfirm` 是否在取消回调中被置为 false。
- 如果删除记录后再次进入写记录页仍是编辑模式，优先检查 `AppStorage.selectedEntryId` 是否已清空。
- 如果删除整本书后返回书架仍显示旧卡片，优先确认书架页 `aboutToAppear` 是否重新查询数据库。

### 21.15 2026-08-03：PDF 导出基础链路与导出历史

实现范围：

- 新增 `services/repository/ExportRecordRepository.ets`，提供导出记录新增、删除、查询全部、按书查询能力。
- 新增 `utils/FileNameUtils.ets`，清洗书名中的非法路径字符并限制文件名长度。
- 新增 `services/export/PdfExportService.ets`，打通导出数据收集、文件名生成、目标路径生成和导出历史写入链路。
- 修改 `pages/journal/bookJournalPage.ets`，将“导出为 PDF”按钮接入 `PdfExportService.exportBookJournal()`。
- 导出成功后写入 `export_records` 表，返回导出结果提示。

排查备注：

- 当前阶段尚未接入真实 PDF 文件绘制和沙箱文件写入，只先生成导出历史记录和目标路径。
- 如果导出提示“这本书还没有可导出的阅读记录”，说明当前书籍下 `reading_entries` 为空。
- 如果导出记录写入失败，优先检查数据库是否已初始化，以及 `export_records` 表是否创建成功。
- 后续真正实现 PDF 文件时，应在 `PdfExportService` 内部补充文件创建、文本排版和分页逻辑，页面入口无需大改。

### 21.16 2026-08-03：导出管理页与我的页入口

实现范围：

- 新增 `pages/export/exportManagerPage.ets`，展示 `export_records` 表中的真实导出历史。
- 导出管理页调用 `ExportRecordRepository.queryAllRecords()` 加载全部导出记录。
- 导出管理页根据导出记录的 `bookId` 调用 `BookRepository.queryBookById()` 补充书籍名称。
- 导出管理页展示文件名、所属书籍、纸张规格、创建日期和目标路径。
- 导出管理页支持删除导出记录，调用 `ExportRecordRepository.deleteRecord(recordId)`。
- 修改 `feature/mePage.ets`，让“导出管理”菜单跳转 `PAGE_PATH.EXPORT_MANAGER_PAGE`。

排查备注：

- 如果导出管理页为空，优先确认是否已经在书籍日记本页执行过“导出为 PDF”。
- 如果记录显示“已删除书籍”，说明导出历史中的 `bookId` 在 `books` 表中找不到对应书籍。
- 当前删除导出记录只删除数据库历史，不删除真实文件；因为 PDF 文件绘制和实际文件写入还未接入。
- 如果我的页点击“导出管理”无反应，优先检查 `openMenuPage()` 是否匹配菜单 title，以及 `PAGE_PATH.EXPORT_MANAGER_PAGE` 是否与页面 `@HMRouter` 一致。

### 21.17 2026-08-03：外观设置偏好持久化

实现范围：

- 新增 `services/repository/AppSettingRepository.ets`，封装 `app_settings` 表的 key-value 读写。
- `AppSettingRepository` 新增 `getThemeMode()` 和 `saveThemeMode(mode)`，用于主题模式偏好。
- 新增 `pages/settings/appearanceSettingsPage.ets`，提供“跟随系统 / 晨雾浅色 / 夜幕深色”三个选项。
- 外观设置页读取并保存 `SettingKey.THEME_MODE`，保存后展示轻量提示。
- 修改 `feature/mePage.ets`，让“外观设置”菜单跳转 `PAGE_PATH.SETTINGS_PAGE`。

排查备注：

- 当前阶段只持久化主题偏好，尚未把主题模式应用到全局页面颜色。
- 如果外观设置页始终显示“跟随系统”，优先检查 `app_settings` 表是否写入 `theme_mode`。
- 如果保存失败，优先检查数据库初始化和 `AppSettingRepository.saveSetting()` 的 insert/update 逻辑。
- 后续接入全局换肤时，可从 `AppSettingRepository.getThemeMode()` 读取偏好并同步到 AppStorage 或全局 Theme Store。

### 21.18 2026-08-03：备份基础快照与备份页面

实现范围：

- 新增 `services/backup/BackupService.ets`，生成应用核心数据的 JSON 快照。
- 备份快照包含版本号、生成时间、书籍、阅读记录、导出历史和已知应用设置。
- 新增 `PAGE_PATH.BACKUP_PAGE` 路由常量。
- 新增 `pages/settings/backupPage.ets`，提供生成备份快照入口、建议文件名和 JSON 预览。
- 修改 `feature/mePage.ets`，让“备份与恢复”菜单跳转备份页面。

排查备注：

- 当前阶段只生成 JSON 文本和建议文件名，尚未接入真实文件写入、分享和文件选择。
- 如果备份快照为空，优先确认 `books`、`reading_entries`、`export_records` 和 `app_settings` 表是否已有数据。
- 如果备份失败，优先检查各 Repository 查询方法和数据库初始化状态。
- 恢复导入暂未开放，后续需要补文件选择、版本校验、冲突处理和数据库事务写入。

### 21.19 2026-08-03：通用文本文件写入与备份落盘

实现范围：

- 新增 `services/storage/FileStorageService.ets`，封装应用沙箱文本文件写入能力。
- 修改 `entryability/EntryAbility.ets`，启动时将 `this.context.filesDir` 写入 `AppStorage.appFilesDir`。
- `FileStorageService.saveText()` 支持按相对路径保存文本，并自动创建父目录。
- 修改 `BackupService.createBackupSnapshot()`，生成 JSON 快照后写入 `backups/啡阅备份_yyyy-MM-dd.json`。
- 修改 `backupPage.ets`，展示真实保存文件路径和 JSON 预览。

排查备注：

- 如果提示“文件目录未初始化”，优先检查 `EntryAbility.onCreate()` 是否执行并写入 `AppStorage.appFilesDir`。
- 如果文件保存失败，优先检查 `FileStorageService.ensureParentDir()` 是否成功创建 `backups` 目录。
- 当前文件写入只用于文本内容，后续 PDF 二进制写入建议在同一服务中扩展 `saveBuffer` 或专用 PDF 写入方法。
- 备份文件当前保存在应用沙箱，后续分享给用户还需要接入系统分享或文件选择器。

### 21.20 2026-08-03：PDF 导出文本落盘基础版

实现范围：

- 修改 `services/export/PdfExportService.ets`，导出时生成书籍日记本文本内容。
- 导出内容包含书名、作者、纸张规格、导出时间、记录数以及每条阅读记录的日期、心情、页码、咖啡和感悟。
- `PdfExportService` 接入 `FileStorageService.saveText()`，将导出内容写入 `exports/pdf/书名_yyyy-MM-dd.pdf`。
- 文件保存成功后才调用 `ExportRecordRepository.insertRecord()` 写入导出历史。
- 导出历史的 `filePath` 改为真实沙箱完整路径。

排查备注：

- 当前 `.pdf` 文件内容仍是 UTF-8 文本占位，并不是真正 PDF 二进制格式。
- 如果导出管理页出现记录但文件打不开为 PDF，属于当前阶段限制；后续需要接入真实 PDF 绘制。
- 如果导出失败并提示文件目录未初始化，优先检查 `AppStorage.appFilesDir` 是否已在启动时写入。
- 后续实现真实 PDF 时，可复用当前数据组装逻辑，只替换文本写入为 PDF 绘制和二进制写入。

### 21.21 2026-08-03：导出文件状态检查

实现范围：

- 修改 `services/storage/FileStorageService.ets`，新增 `exists(filePath)` 文件存在判断。
- 修改 `pages/export/exportManagerPage.ets`，导出历史展示模型新增 `fileExists` 字段。
- 导出管理页加载历史时调用 `FileStorageService.exists(record.filePath)` 检查文件是否仍存在。
- 导出记录卡片新增“文件可用 / 文件缺失”状态标签。

排查备注：

- 如果刚导出的文件显示“文件缺失”，优先检查 `export_records.file_path` 是否为完整沙箱路径。
- 如果所有文件都显示缺失，优先确认 `FileStorageService.saveText()` 返回的 `filePath` 是否已写入导出历史。
- 当前删除导出记录只删除数据库历史，不删除真实文件；后续可扩展 `FileStorageService.deleteFile()`。
- 文件状态检查只基于路径存在性，不校验文件内容是否是真正 PDF 格式。

### 21.22 2026-08-03：导出文件与历史同步删除

实现范围：

- 修改 `services/storage/FileStorageService.ets`，新增 `FileDeleteResult` 和 `deleteFile(filePath)`。
- `deleteFile()` 在路径为空或文件不存在时返回成功，允许导出管理页继续清理数据库历史。
- 修改 `pages/export/exportManagerPage.ets`，删除导出记录时先删除真实文件，再删除 `export_records` 历史记录。
- 如果真实文件删除失败，页面保留数据库历史并展示错误，方便用户重试。

排查备注：

- 如果删除导出记录后文件仍存在，优先检查 `item.filePath` 是否为真实沙箱完整路径。
- 如果文件已经缺失但历史无法删除，优先检查 `ExportRecordRepository.deleteRecord()`。
- 当前删除没有二次确认，后续可以复用书籍日记本的底部确认面板模式。
- 文件删除只处理导出管理页记录对应的单个文件，不会清理空目录。

### 21.23 2026-08-03：接入 PDFKit 生成真实 PDF

实现范围：

- 修改 `services/export/PdfExportService.ets`，引入 `@kit.PDFKit` 的 `pdfService`。
- 使用 `new pdfService.PdfDocument()` 创建 PDF 文档，并调用 `createDocument(width, height)` 初始化页面。
- 使用 `PdfPage.addTextObject()` 写入书籍日记本标题、书籍信息和阅读记录内容。
- 实现基础分页逻辑：当当前页剩余高度不足时，调用 `insertBlankPage()` 新增页面并继续写入。
- 使用系统字体 `/system/fonts/HarmonyOS_Sans.ttf` 设置中文文本样式。
- PDF 保存成功后再写入 `ExportRecordRepository`，导出历史中的 `filePath` 仍为真实沙箱完整路径。
- 修改 `FileStorageService`，新增 `buildFullPath(relativePath)` 和 `ensureParentDirForFile(fullPath)`，供 PDFKit 保存前准备目录。

排查备注：

- 如果编译提示 `@kit.PDFKit` 或 `pdfService` 不存在，说明当前 SDK 未启用或版本不包含 PDFKit，需要在工程依赖或 API 版本中确认 PDFKit 可用性。
- 如果编译提示 `insertBlankPage` 参数不匹配，应以当前 SDK 的 PDFKit API 为准调整插入空白页调用。
- 如果 PDF 保存失败，优先检查 `AppStorage.appFilesDir` 是否已初始化，以及 `exports/pdf` 父目录是否创建成功。
- 当前排版是基础文本分页，暂未支持图片、水印、目录、页码和复杂换行测量。

### 21.24 2026-08-03：导出文件删除二次确认

实现范围：

- 修改 `pages/export/exportManagerPage.ets`，删除导出记录前增加底部二次确认面板。
- 页面新增 `showDeleteConfirm` 和 `pendingDeleteItem`，记录待删除的导出历史项。
- 点击“删除记录”时只打开确认面板，不立即删除文件或数据库历史。
- 确认删除后才调用 `FileStorageService.deleteFile()` 删除真实文件，并调用 `ExportRecordRepository.deleteRecord()` 删除导出历史。
- 根据文件是否存在展示不同确认文案：文件可用时提示会同步删除 PDF 文件，文件缺失时提示只删除历史记录。

排查备注：

- 如果点击“删除记录”没有立即删除，属于预期行为，需要在确认面板中再次点击“确认删除”。
- 如果确认删除后文件仍存在，优先检查 `pendingDeleteItem.filePath` 是否是沙箱完整路径。
- 如果文件缺失但历史删除失败，优先检查 `ExportRecordRepository.deleteRecord()`。
- 当前确认面板只用于导出管理页，后续可抽出通用危险操作确认组件。

### 21.25 2026-08-03：PDF 文件详情与预览占位页

实现范围：

- 新增 `pages/export/pdfPreviewPage.ets`，作为导出文件详情和后续 PDF 预览入口。
- PDF 文件详情页通过 `AppStorage.previewPdfFileName` 和 `AppStorage.previewPdfFilePath` 获取导出管理页传入的文件信息。
- 页面使用 `FileStorageService.exists(filePath)` 重新检查文件状态，展示“文件可用 / 文件缺失”。
- 页面展示文件名、沙箱保存路径和模拟器预览限制说明。
- 修改 `pages/export/exportManagerPage.ets`，点击导出记录卡片时写入预览参数并跳转 `PAGE_PATH.PDF_PREVIEW_PAGE`。

排查备注：

- 如果点击导出记录没有进入详情页，优先检查 `openPdfPreviewPage()` 是否触发，以及 `PAGE_PATH.PDF_PREVIEW_PAGE` 是否和页面 `@HMRouter` 一致。
- 如果详情页显示文件缺失但列表显示可用，优先检查进入详情页后文件是否被删除或路径参数是否被覆盖。
- 当前页面只是预览占位和文件详情展示，尚未接入系统 PDF 预览组件或外部打开能力。
- 后续真机验证 PDFKit 输出文件可打开后，可在当前页面继续增加“打开 PDF / 分享 PDF”按钮。

### 21.26 2026-08-03：主题偏好同步到全局状态

实现范围：

- 新增 `services/theme/ThemePreferenceService.ets`，统一管理主题偏好的读取和 AppStorage 同步。
- `ThemePreferenceService.loadThemeModeToStorage()` 从 `AppSettingRepository.getThemeMode()` 读取数据库偏好，并写入 `AppStorage.currentThemeMode`。
- `ThemePreferenceService.syncThemeModeToStorage(mode)` 用于外观设置保存后立即同步当前主题状态。
- 修改 `entryability/EntryAbility.ets`，数据库初始化成功后加载主题偏好到全局状态。
- 修改 `pages/settings/appearanceSettingsPage.ets`，保存主题模式后同步更新 `AppStorage.currentThemeMode`。

排查备注：

- 当前阶段完成的是主题偏好全局状态同步，尚未批量替换所有页面硬编码颜色。
- 如果 `currentThemeMode` 始终为 `system`，优先检查 `app_settings.theme_mode` 是否保存成功。
- 如果应用启动后主题偏好未加载，优先检查 `DatabaseManager.init()` 是否成功，以及 `ThemePreferenceService.loadThemeModeToStorage()` 是否执行。
- 后续页面适配深色模式时，应优先从 `ThemePreferenceService.getCurrentThemeMode()` 或 `AppStorage.currentThemeMode` 获取当前偏好。

### 21.27 2026-08-03：外观设置页主题适配基础版

实现范围：

- 修改 `services/theme/ThemePreferenceService.ets`，新增 `getCurrentPalette()`，根据当前主题模式返回 `BrewTheme.light` 或 `BrewTheme.dark`。
- 修改 `pages/settings/appearanceSettingsPage.ets`，新增 `renderMode`，用于保存后立即触发页面视觉刷新。
- 外观设置页背景色、卡片色、主文本色、次文本色、主色和加载卡片色改为从当前 `ThemePalette` 获取。
- 保存主题模式后同时更新 `selectedMode`、`renderMode` 和 `AppStorage.currentThemeMode`。

排查备注：

- 当前只适配外观设置页，用于验证主题偏好是否能即时影响页面视觉。
- `system` 模式当前仍按浅色处理，后续需要接系统深浅色状态后再完善跟随系统。
- 如果保存深色后页面没有变化，优先检查 `renderMode` 是否更新，以及 `ThemePreferenceService.syncThemeModeToStorage()` 是否写入成功。
- 后续可按同样方式逐步适配我的页、导出管理页、备份页和书籍日记本页。

### 21.28 2026-08-03：新页面主题适配与咖伴洞察

实现范围：

- 修改 `pages/export/pdfPreviewPage.ets`，接入 `ThemePreferenceService.getCurrentPalette()`，适配页面背景、卡片、主文本、次文本和主色。
- 修改 `pages/settings/backupPage.ets`，接入当前主题调色板，适配备份与恢复页面主要颜色。
- 根据 v2.3 需求诊断，优先落地“咖啡维度太薄”的低成本增强。
- 修改 `feature/mePage.ets`，新增 `CoffeeInsight` 数据结构和 `coffeeInsight` 状态。
- 我的页新增“咖伴”卡片，展示最常出现的咖啡、出现次数、最常搭配心情和一句情绪化总结。
- 咖伴洞察完全基于已有 `reading_entries.coffee` 和 `reading_entries.mood` 聚合，不新增数据库字段。

排查备注：

- 如果 PDF 详情页或备份页主题不生效，优先检查 `AppStorage.currentThemeMode` 是否已更新。
- 如果咖伴卡片始终为空，优先确认阅读记录中 `coffee` 字段是否非空。
- 当前咖啡名称按完全相等聚合，`热美式` 和 `美式` 会被视为不同咖啡；后续可增加咖啡标准化。
- v2.3 中“读完仪式”和“金句标记”仍未实现，后续建议分别作为独立任务推进。

### 21.29 2026-08-03：我的阅读人格画像与书籍咖啡总结

实现范围：

- 修改 `feature/mePage.ets`，新增 `ReadingPersona` 和 `readingPersona`，在我的页顶部展示阅读人格画像文案。
- 阅读人格画像基于记录总数、在读/已读书籍、主导心情和咖伴洞察生成。
- 修改 `pages/journal/bookJournalPage.ets`，新增 `BookCoffeeSummary` 和 `coffeeSummary`。
- 书籍日记本加载当前书籍阅读记录时，聚合该书咖啡字段，统计最常出现的咖啡。
- 摘要区新增一句情绪化咖啡总结，例如“这本书陪你走过 12 个阅读时刻，配了 9 杯咖啡，其中热美式出现了 5 次”。

排查备注：

- 如果我的页画像文案不准确，优先检查 `buildMoodStats()` 是否先于 `buildReadingPersona()` 执行。
- 如果书籍咖啡总结显示空状态，优先确认当前书籍的阅读记录中 `coffee` 字段是否非空。
- 咖啡总结当前按完全相等聚合，暂未做同义词归一化。
- 后续读完仪式可以复用本轮咖啡聚合逻辑和主导心情统计逻辑。

### 21.29 2026-08-03：我的阅读人格画像

实现范围：

- 修改 `feature/mePage.ets`，新增 `ReadingPersona` 数据结构和 `readingPersona` 状态。
- 新增 `buildReadingPersona(entries, coffeeInsight)`，基于总阅读记录数、已读/在读书籍、主导心情和咖伴洞察生成一句画像文案。
- 在“我的啡阅”头部下方新增阅读人格画像文案卡片，让我的页从纯统计入口变成情感回报入口。
- 画像文案复用已有统计数据，不新增数据库字段和额外查询。

排查备注：

- 如果画像文案始终显示空状态，优先确认 `ReadingEntryRepository.queryAllEntries()` 是否返回真实记录。
- 如果主导心情不准确，优先检查 `buildMoodStats()` 是否先于 `buildReadingPersona()` 执行。
- 当前画像基于所有历史记录聚合，后续可按 v2.3 需求改为近 3 个月或按自然月生成更有时间感的表达。

执行上建议严格参考第 16 节计划表逐项完成，每个阶段都形成可运行、可验证的功能增量，避免一次性堆叠所有需求导致集成风险。