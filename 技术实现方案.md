# 啡阅（BrewRead）技术实现方案与需求修改建议

> 基于 `需求.md` v2.0（精简本地版）整理  
> 平台：HarmonyOS NEXT  
> 技术栈：ArkTS + ArkUI + RelationalStore + 应用沙箱文件存储

---

## 1. 需求文档整体评价

当前需求文档定位清晰，产品边界明确，核心体验聚焦在「一本书 + 一杯咖啡 + 一段心情 + PDF 成册」。整体非常适合作为本地单机 MVP 开发，但在进入工程实现前，建议补充部分关键规则与技术约束，尤其是数据一致性、权限、图片与 PDF 生成、备份恢复、扫码 ISBN 的可行性边界。

---

## 2. 需求修改意见

### 2.1 建议修正文档中的页面数量不一致

需求中写到「所有页面（共10个）」，但表格实际列出了 12 个页面：

- 书架：3 个
- 记录：4 个
- 日历：2 个
- 我的：3 个

建议修改为：

> 所有页面（共12个）

或者压缩页面，将「导出管理」「设置」合并到「我的首页」，再保持 10 个页面。

推荐方案：保留 12 个页面，因为功能边界更清晰，也方便后续路由管理。

---

### 2.2 建议明确 MVP 范围，不建议“首版即全部”

需求中写到「首版即全部，一次做完」，但包含以下复杂功能：

- 图片拍照与本地存储
- ISBN 扫描与 OCR
- PDF 自绘排版
- 导出历史管理
- 数据备份/恢复
- 日历热力图

这些功能一次性完成会显著增加开发风险。建议将 MVP 拆成两个阶段：

#### MVP 1.0 必做

- 书籍增删改查
- 阅读记录增删改查
- 本地数据库持久化
- 书籍详情与阅读时间线
- 日历月视图基础展示
- 单本书 PDF 导出基础版

#### MVP 1.1 增强

- 封面拍照
- 阅读记录照片
- 导出历史
- 数据备份/恢复
- PDF 模板优化

#### MVP 1.2 可选

- ISBN 扫描识别
- OCR 自动提取 ISBN
- PDF 多主题/字体选择
- 更丰富统计图表

---

### 2.3 建议弱化或移除「ISBN 本地 OCR 自动填充」

当前需求写到：

> 扫描ISBN条形码自动填充信息（本地OCR，无需网络）

这里有两个问题：

1. **扫描 ISBN 条形码本身不等于获取书籍信息**。条形码只能识别 ISBN 编号，书名、作者、出版社等元数据通常需要联网查询书籍数据库。
2. 在完全离线模式下，即使识别到 ISBN，也无法凭空自动填充书名和作者，除非内置庞大的书籍数据库，这不符合轻量本地应用定位。

建议改成：

> 可选：扫描 ISBN 条形码，仅自动填入 ISBN 号；书名、作者仍由用户手动填写。

并在数据模型中增加 `isbn` 字段。

---

### 2.4 建议补充数据校验规则

当前字段说明比较简洁，但缺少输入约束。建议补充：

#### 书籍

| 字段 | 建议规则 |
|---|---|
| 书名 | 必填，1-100 字 |
| 作者 | 可选，最多 80 字 |
| 总页数 | 可选，1-99999 |
| ISBN | 可选，10 或 13 位数字，允许连字符存储前先规范化 |
| 状态 | reading / finished / want，默认 reading |

#### 阅读记录

| 字段 | 建议规则 |
|---|---|
| 关联书籍 | 必填，必须存在 |
| 起始页 | 可选，但填写时必须大于 0 |
| 结束页 | 可选，必须大于等于起始页 |
| 页码范围 | 不应超过书籍总页数，允许用户确认后保存 |
| 心情 | 必填，默认 calm |
| 咖啡 | 可选，最多 50 字 |
| 感悟 | 必填，1-5000 字 |
| 日期时间 | 默认当前时间，可手动修改，不能晚于当前时间太久，可允许补记过去日期 |
| 照片 | 可选，建议压缩后保存 |

---

### 2.5 建议补充删除策略

需要明确删除书籍时如何处理关联记录。

推荐策略：

- 删除书籍前弹出二次确认。
- 删除书籍会级联删除该书所有阅读记录。
- 同时删除该书封面图片与记录照片。
- 若后续要降低误删风险，可增加「软删除」字段，但 MVP 可以直接硬删除。

---

### 2.6 建议补充进度计算规则

书架中展示「已读 236/384 页」或「进度 45%」，需要明确计算来源。

推荐规则：

- 优先取该书所有阅读记录中最大的 `pageTo` 作为当前阅读页。
- 如果没有记录，当前页显示为 0。
- 若书籍总页数存在，则显示百分比。
- 当最大 `pageTo` 大于等于 `totalPages` 时，可提示用户是否将书籍状态改为「已读完」。

---

### 2.7 建议补充 PDF 导出失败和异常处理

PDF 是关键功能，需要在需求中补充失败场景：

- 内容过长自动分页。
- 图片加载失败时显示占位或跳过图片。
- 导出中禁止重复点击。
- 磁盘空间不足时提示用户。
- 导出成功后提供「预览 / 分享 / 保存到文件」入口。
- 导出的 PDF 文件名建议为：`书名_阅读日记_YYYYMMDD_HHmm.pdf`。

---

### 2.8 建议明确备份恢复格式

「数据备份/恢复」在设置中出现，但技术方案未展开。建议定义为：

- 备份格式：一个 `.zip` 文件。
- 内容包括：
  - `books.json`
  - `entries.json`
  - `images/` 图片目录
  - `metadata.json`，包含应用版本、导出时间、数据版本
- 恢复策略：
  - 先校验备份版本。
  - 再导入到临时表或内存校验。
  - 最后由用户确认覆盖或合并。

MVP 可先做「导出备份」，恢复放到后续版本。

---

## 3. 推荐技术架构

### 3.1 分层结构

建议采用轻量分层架构：

```text
UI 页面层（ArkUI Pages / Components）
  ↓
状态与业务层（ViewModel / Store）
  ↓
领域服务层（BookService / EntryService / ExportService）
  ↓
数据访问层（Repository / DAO）
  ↓
本地存储层（RelationalStore + Sandbox Files）
```

这样可以保证 UI 与数据库、文件系统、PDF 生成逻辑解耦，后续维护成本更低。

---

### 3.2 推荐目录结构

```text
entry/src/main/ets/
├── common/
│   ├── constants/
│   ├── enums/
│   ├── utils/
│   └── validators/
├── models/
│   ├── Book.ets
│   ├── ReadingEntry.ets
│   ├── ExportRecord.ets
│   └── Setting.ets
├── database/
│   ├── DatabaseManager.ets
│   ├── migrations/
│   └── dao/
│       ├── BookDao.ets
│       ├── ReadingEntryDao.ets
│       └── ExportRecordDao.ets
├── services/
│   ├── BookService.ets
│   ├── ReadingEntryService.ets
│   ├── CalendarService.ets
│   ├── StatisticsService.ets
│   ├── ImageService.ets
│   ├── PdfExportService.ets
│   └── BackupService.ets
├── viewmodels/
│   ├── BookshelfViewModel.ets
│   ├── JournalViewModel.ets
│   ├── CalendarViewModel.ets
│   └── MeViewModel.ets
├── pages/
│   ├── bookshelf/
│   ├── journal/
│   ├── calendar/
│   └── me/
└── components/
    ├── BookCard.ets
    ├── EntryCard.ets
    ├── MoodSelector.ets
    ├── CalendarHeatMap.ets
    └── EmptyState.ets
```

---

## 4. 数据库设计建议

需求文档中只设计 2 张表，业务上基本够用。但考虑导出历史、设置、备份恢复、ISBN，建议扩展为 4 张表。

### 4.1 books 表

```sql
CREATE TABLE IF NOT EXISTS books (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  author TEXT,
  isbn TEXT,
  total_pages INTEGER,
  cover_path TEXT,
  status TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
```

建议索引：

```sql
CREATE INDEX IF NOT EXISTS idx_books_status ON books(status);
CREATE INDEX IF NOT EXISTS idx_books_created_at ON books(created_at DESC);
```

---

### 4.2 reading_entries 表

```sql
CREATE TABLE IF NOT EXISTS reading_entries (
  id TEXT PRIMARY KEY,
  book_id TEXT NOT NULL,
  date INTEGER NOT NULL,
  page_from INTEGER,
  page_to INTEGER,
  mood TEXT NOT NULL,
  coffee TEXT,
  content TEXT NOT NULL,
  photo_path TEXT,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  FOREIGN KEY(book_id) REFERENCES books(id) ON DELETE CASCADE
);
```

建议索引：

```sql
CREATE INDEX IF NOT EXISTS idx_entries_book_id ON reading_entries(book_id);
CREATE INDEX IF NOT EXISTS idx_entries_date ON reading_entries(date DESC);
CREATE INDEX IF NOT EXISTS idx_entries_book_date ON reading_entries(book_id, date DESC);
```

---

### 4.3 export_records 表

用于「导出管理」。

```sql
CREATE TABLE IF NOT EXISTS export_records (
  id TEXT PRIMARY KEY,
  book_id TEXT NOT NULL,
  file_path TEXT NOT NULL,
  paper_size TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  FOREIGN KEY(book_id) REFERENCES books(id) ON DELETE CASCADE
);
```

---

### 4.4 app_settings 表

```sql
CREATE TABLE IF NOT EXISTS app_settings (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at INTEGER NOT NULL
);
```

建议存储：

| key | value 示例 |
|---|---|
| default_paper_size | A5 |
| default_pdf_font | serif |
| data_version | 1 |

---

## 5. 核心功能技术实现建议

### 5.1 书架模块

#### 实现要点

- 使用 `Tabs` 或分段控件实现「在读 / 已读完 / 想读」切换。
- 列表数据按 `status` 查询。
- 书籍卡片展示：封面、书名、作者、进度、记录数。
- 记录数通过聚合查询或进入页面时批量查询得到。

#### 进度计算

```text
currentPage = max(reading_entries.page_to where book_id = 当前书籍)
progress = currentPage / totalPages
```

如果 `totalPages` 为空或 0，只展示「已读至第 X 页」，不展示百分比。

---

### 5.2 阅读记录模块

#### 实现要点

- 写记录页面需要先加载书籍列表。
- 如果书架为空，应引导用户先添加书籍。
- 保存记录时进行字段校验。
- 保存成功后刷新记录时间线和书籍进度。
- 编辑记录时复用写记录页面组件。

#### 表单建议

- 书籍选择：下拉选择或弹窗列表。
- 页码输入：两个数字输入框。
- 心情选择：固定 5 个枚举。
- 咖啡输入：普通文本框，保留历史输入可作为后续优化。
- 感悟输入：多行文本，建议支持 5000 字以内。
- 照片：调用系统相机或相册选择，保存到应用沙箱。

---

### 5.3 日记本模块

#### 实现要点

- 本质是 `reading_entries` 按 `book_id` 过滤的时间线。
- 页面顶部展示书籍基础信息和统计摘要。
- 记录列表默认按时间倒序，PDF 导出时建议按时间正序。
- 单条卡片支持展开全文、编辑、删除。

#### 统计摘要

```text
共读次数 = 当前书籍记录数
配咖啡次数 = coffee 非空记录数
阅读跨度 = 第一条记录日期 到 最后一条记录日期
累计字数 = content 字符数求和
```

---

### 5.4 日历热力图模块

#### 数据聚合

按日期聚合阅读记录：

```text
dateKey = yyyy-MM-dd
entryCount = 当天记录数
wordCount = 当天 content 字数总和
coffeeCount = coffee 非空数量
mainMood = 当天最后一条记录的 mood 或出现次数最多的 mood
```

#### 热力强度建议

```text
intensity = min(4, ceil(wordCount / 300))
```

或：

```text
intensity = min(4, entryCount)
```

推荐优先使用 `wordCount`，更符合「写了多少」的记录感。

#### 页面交互

- 左右滑切换月份。
- 点击有记录日期：底部展示当天摘要。
- 点击无记录日期：展示「补记这一天」入口，跳转写记录页并预填日期。

---

### 5.5 我的模块

#### 基础统计

可由数据库动态聚合得到，无需单独存储：

- 总书籍数
- 在读数
- 已读完数
- 想读数
- 总记录数
- 有咖啡记录数
- 阅读天数
- 心情分布

#### 导出管理

建议 PDF 导出成功后写入 `export_records` 表。导出历史列表从该表读取，并校验文件是否仍存在。

---

## 6. 图片存储方案

### 6.1 存储位置

所有图片保存到应用沙箱目录，建议目录：

```text
files/
├── covers/
│   └── {bookId}.jpg
├── entries/
│   └── {entryId}.jpg
└── exports/
    └── {exportId}.pdf
```

### 6.2 图片处理建议

- 拍照后复制到应用沙箱，不直接保存外部 URI。
- 图片统一压缩为 JPEG。
- 封面图建议宽度 600px 左右。
- 阅读场景图建议宽度 1280px 左右。
- 删除书籍或记录时，同时删除对应图片文件。

---

## 7. PDF 导出技术方案

### 7.1 推荐实现路径

需求中写到「自绘排版（Canvas导出）」。工程上建议拆成三层：

```text
PdfExportService
  ├── 查询书籍与记录数据
  ├── 构造 PDF 文档模型
  ├── 调用 PdfLayoutEngine 排版
  └── 保存 PDF 文件并写入导出历史

PdfLayoutEngine
  ├── 页面尺寸计算
  ├── 文本换行
  ├── 自动分页
  ├── 图片缩放
  └── 页眉页脚绘制

PdfFileWriter
  ├── 创建文件
  ├── 写入 PDF 内容
  └── 返回沙箱路径或分享 URI
```

### 7.2 排版规则建议

#### 页面尺寸

| 纸张 | 尺寸 |
|---|---|
| A4 | 595 × 842 pt |
| A5 | 420 × 595 pt |

#### 样式

| 元素 | 建议 |
|---|---|
| 背景 | 米色 `#F5F0E8` |
| 正文色 | 深焙黑 `#2C1810` |
| 标题色 | 咖啡棕 `#6B4226` |
| 页边距 | A4 48pt，A5 36pt |
| 正文字号 | A4 11-12pt，A5 10-11pt |
| 行高 | 字号的 1.6 倍 |

### 7.3 PDF 内容顺序

1. 封面页
   - 书名
   - 作者
   - 阅读年份
   - 共读次数
   - 配咖啡次数
   - 生成日期
2. 正文页
   - 日期
   - 心情
   - 咖啡
   - 页码范围
   - 阅读感悟全文
   - 图片（如有）
3. 结束页（可选）
   - 「这本书的阅读旅程到这里告一段落」

### 7.4 自动分页策略

推荐使用「流式布局」：

```text
当前页面 y = topMargin
逐个绘制 block
  如果 block 高度 <= 剩余高度：绘制
  如果 block 高度 > 剩余高度：新建页面
  如果 block 是长文本：按行切分，跨页绘制
```

长文本必须先执行文字测量与换行，避免文字溢出页面。

---

## 8. 权限与系统能力建议

### 8.1 可能需要的权限

| 功能 | 权限/能力 |
|---|---|
| 拍照封面 | 相机权限 |
| 拍阅读场景照片 | 相机权限 |
| 从相册选择图片 | 媒体访问能力 |
| 保存 PDF 到用户选择位置 | 文件选择器 / 保存 Picker |
| 分享 PDF | 系统分享能力 |
| 打印 PDF | 系统打印或分享至打印应用 |

### 8.2 权限申请原则

- 不在首次启动时一次性申请所有权限。
- 用户点击「拍照」时再申请相机权限。
- 用户点击「选择图片」时再请求媒体访问。
- 权限被拒绝时提供手动输入或跳过路径。

---

## 9. 状态管理建议

App 规模不大，不建议引入过重架构。推荐：

- 页面级状态：使用 ArkUI 的状态装饰器管理。
- 跨页面刷新：通过服务层查询最新数据，或使用简单事件通知。
- 数据源以数据库为准，不长期缓存复杂对象。

建议每个页面进入时刷新关键数据，保证编辑/删除后返回页面显示正确。

---

## 10. 错误处理与空状态设计

### 10.1 空状态

| 场景 | 文案建议 |
|---|---|
| 书架为空 | 还没有书，添加第一本开始记录吧 |
| 记录为空 | 今天还没有写下阅读感悟 |
| 某书无记录 | 这本书还没有阅读日记 |
| 日历当月无记录 | 这个月还没有阅读足迹 |
| 导出历史为空 | 导出的阅读手账会出现在这里 |

### 10.2 常见错误

| 错误 | 处理建议 |
|---|---|
| 数据库初始化失败 | 提示重启应用，保留错误日志 |
| 保存失败 | Toast 提示，并保留表单内容 |
| 图片保存失败 | 提示图片未保存，但允许保存文本记录 |
| PDF 导出失败 | 显示失败原因，允许重试 |
| 文件不存在 | 导出历史中标记为「文件已不存在」 |

---

## 11. 开发实施计划

### 阶段 1：工程基础与数据层

- 创建 HarmonyOS 工程基础结构。
- 定义 `Book`、`ReadingEntry`、`ExportRecord`、`Setting` 模型。
- 初始化 RelationalStore。
- 实现 DAO 与基础 CRUD。
- 增加数据库版本迁移能力。

### 阶段 2：书架与记录闭环

- 实现书架首页。
- 实现添加/编辑/删除书籍。
- 实现写记录/编辑记录。
- 实现全部记录时间线。
- 实现书籍详情中的记录列表。

### 阶段 3：日历与统计

- 实现按月聚合记录。
- 实现日历热力图组件。
- 实现日期详情页。
- 实现我的首页统计概览。

### 阶段 4：图片能力

- 实现拍照/选图。
- 实现图片复制、压缩、沙箱保存。
- 实现图片预览与删除。

### 阶段 5：PDF 导出

- 实现 PDF 数据查询。
- 实现 PDF 排版模型。
- 实现封面页。
- 实现正文自动分页。
- 实现图片插入。
- 实现保存、分享、导出历史。

### 阶段 6：设置、备份与打磨

- 默认纸张设置。
- 默认字体设置。
- 数据备份导出。
- 空状态、加载态、错误态完善。
- 真机测试与性能优化。

---

## 12. 测试建议

### 12.1 数据测试

- 新增、编辑、删除书籍。
- 新增、编辑、删除阅读记录。
- 删除书籍后关联记录是否正确删除。
- 页码非法输入是否被拦截。
- App 重启后数据是否仍存在。

### 12.2 日历测试

- 单日单条记录。
- 单日多条记录。
- 跨月记录。
- 闰年 2 月。
- 月份首日为周一/周日等不同布局。

### 12.3 PDF 测试

- 无记录导出。
- 短记录导出。
- 超长文本导出。
- 多张图片导出。
- A4/A5 两种纸张导出。
- 文件名包含特殊字符的书名导出。

### 12.4 文件测试

- 图片文件丢失。
- PDF 文件被外部删除。
- 沙箱空间不足。
- 备份文件版本不匹配。

---

## 13. 技术风险与规避建议

| 风险 | 影响 | 建议 |
|---|---|---|
| PDF 自绘排版复杂 | 高 | 先实现基础纯文本版，再支持图片和样式 |
| 离线 ISBN 自动填充不可行 | 中 | 改为仅识别 ISBN 号，不承诺自动填书名作者 |
| 图片体积过大 | 中 | 统一压缩并限制最大尺寸 |
| 数据误删 | 中 | 删除二次确认，后续可引入备份 |
| 日历热力规则不明确 | 低 | 以字数或记录数定义强度 |
| 备份恢复复杂 | 中 | 首版先做导出备份，恢复延后 |

---

## 14. 建议更新后的核心技术方案摘要

| 层级 | 推荐方案 | 说明 |
|---|---|---|
| 语言 | ArkTS | HarmonyOS NEXT 原生开发 |
| UI | ArkUI 声明式 | 页面与组件拆分 |
| 状态 | 页面状态 + ViewModel | 保持轻量 |
| 数据库 | RelationalStore | books / reading_entries / export_records / app_settings |
| 图片 | 应用沙箱目录 | 封面、记录照片、导出 PDF 分目录存储 |
| PDF | 本地 PDF 导出服务 | 封面 + 正文 + 自动分页 |
| 备份 | JSON + images 打包 | 建议后续实现 zip 备份 |
| 扫码 | 可选本地识别 ISBN | 不承诺离线自动补全书籍信息 |

---

## 15. 优先级建议

如果希望尽快落地，推荐按以下顺序开发：

1. 数据库与模型
2. 书架 CRUD
3. 阅读记录 CRUD
4. 书籍日记本
5. PDF 基础导出
6. 日历热力图
7. 我的统计
8. 图片能力
9. 导出历史
10. 备份恢复
11. ISBN 扫描

其中 PDF 是产品差异化核心，但技术复杂度较高，建议尽早做一个可运行的基础原型，避免最后阶段才发现排版、分页或系统保存能力存在限制。

---

## 16. 结论

`需求.md` 已经具备清晰的产品方向，适合进入设计和开发阶段。正式开发前建议重点调整三点：

1. 修正页面数量与 MVP 范围描述。
2. 明确 ISBN 离线能力边界，避免承诺无法实现的自动书籍信息填充。
3. 补充数据校验、删除策略、PDF 异常处理和备份格式。

技术实现上建议采用轻量分层架构，以 RelationalStore 作为唯一结构化数据源，以应用沙箱管理图片和 PDF 文件，先打通「书籍 → 记录 → 日记本 → PDF 导出」核心闭环，再逐步补齐日历、统计、备份和扫码能力。
